# Terranova 项目

## 项目简介

**Terranova（新大陆）** 是一个基于知识图谱的游戏化学习系统。将课程体系构建为"知识大陆"地图，学生作为探险者通过学习驱散迷雾、开拓新大陆，结合任务系统、成就徽章和社交竞争，实现渐进式个性化学习。

## 核心目标

构建一个 **知识图谱驱动 + 游戏化** 的智能学习辅助系统：
1. 将专业课程体系建模为知识图谱，并以游戏地图形式呈现
2. 通过"探索大陆、驱散迷雾"的游戏机制驱动渐进式学习
3. 基于图谱拓扑关系，动态推荐最优学习路径
4. 通过 LLM 生成知识点讲解、关联分析和学习建议
5. 以任务系统、成就徽章、社交竞争构建完整的用户激励闭环

## 应用场景

- **多专业支持**：不限于 CS，支持任意专业的课程体系（经济学、医学、工学等）
- **首期切入**：计算机科学专业作为示范
- **扩展方向**：其他专业、培训机构课程体系
- **商业模式**：面向高校的教学辅助工具 / 面向学生的个性化学习规划 SaaS

## 技术方案

### 1. 知识图谱构建（Neo4j）

**节点类型**：
- `Domain`：专业/大陆（如计算机科学、经济学、临床医学）
- `Course`：课程/王国（如数据结构、微观经济学）
- `Chapter`：章节/城镇（如"栈与队列"、"市场均衡"）
- `KnowledgePoint`：知识点/地点（如"二叉树遍历"、"供需曲线"）
- `Concept`：概念/术语（如"时间复杂度"、"互斥锁"）

**关系类型**：
- `CONTAINS`：课程包含章节，章节包含知识点
- `PREREQUISITE`：前置依赖（学A之前必须先学B）
- `RELATED_TO`：关联关系（跨课程的知识关联，地图上表现为"暗道"）
- `EXTENDS`：扩展/深入（从基础到进阶）

**示例图谱路径**：
```
[数据结构] -CONTAINS-> [树与二叉树] -CONTAINS-> [二叉树遍历]
[二叉树遍历] -PREREQUISITE-> [递归]
[二叉树遍历] -RELATED_TO-> [图的DFS] (跨章节关联 → 暗道)
[递归] -EXTENDS-> [分治算法]
```

### 2. 知识大陆地图系统（核心玩法）

将知识图谱以游戏地图形式呈现，学生作为"探险者"通过学习来驱散迷雾、开拓地图。

#### 地图层级

| 地图元素 | 知识图谱对应 | 游戏化呈现 |
|---------|------------|-----------|
| **大陆** | Domain（专业） | 选择你的探险大陆 |
| **王国/区域** | Course（课程） | 满足前置课程后解锁，迷雾消散 |
| **城镇** | Chapter（章节） | 进入需完成前序章节 |
| **地点/据点** | KnowledgePoint（知识点） | 最小可交互单元 |
| **道路** | PREREQUISITE 关系 | 连接地点的可见路径 |
| **暗道/彩虹桥** | RELATED_TO 跨课程关联 | 学到某节点后意外发现的跨域通道 |

#### 迷雾机制（Fog of War）

```
完全迷雾  →  看不到任何内容，只知道"这里有一片区域"
边缘可见  →  已掌握节点的邻居露出轮廓（名称可见，内容锁定）
已解锁    →  前置依赖全部满足，可以进入学习
已探索    →  完成学习，地点亮起，周围迷雾进一步消散
已精通    →  地点升级为"据点"，外观变化（如点亮灯塔）
```

每完成一个知识点，周围的迷雾向外扩散一层，产生"想继续探索"的驱动力。

#### 地点交互

点击已解锁的地点后展示：
- **学习讲解**：LLM 生成的结构化讲解
- **练习挑战**：答题/练习提升掌握星级（最高 3 星）
- **查看关联**：高亮地图上的关联节点
- **前置状态**：已完成的前置知识 ✅ 和将解锁的后续地点
- **社交信息**：首位探索者、已到达人数

#### 暗道机制——跨课程惊喜发现

当学生掌握某知识点后，若它与另一门课存在 `RELATED_TO` 关联，地图上动画展示一条隐藏通道被发现：

> "你发现了一条暗道！数据结构的「B+树」与数据库的「索引原理」相通——点击穿越？"

自然引导跨学科学习，增加探索惊喜感。

#### 地图进度可视化

- **鸟瞰视角**：一眼看出哪些区域已开拓、哪些还是迷雾
- **探索率**：如"计算机科学大陆已探索 34%"
- **探索轨迹**：查看自己的学习历史路径
- **目标导航**：选定目标地点，高亮最短路径上所有待学节点

### 3. 任务系统

#### 每日任务
- 复习 1 个旧知识点（防遗忘）
- 学习 1 个新知识点（保持进度）
- 完成后获得额外 EXP 奖励

#### 挑战任务
- 限时完成某条学习路径（如"3 天内从零掌握排序算法"）
- 难度分级，高难度任务奖励更丰厚

#### 支线任务
- 探索跨课程关联（如学了数据库的 B+ 树，推荐去看数据结构里的树）
- 通过暗道发现触发

#### 奖励循环
```
学习知识点 → 获得 EXP + 解锁新节点 → 触发成就 → 获得徽章
    ↑                                                  |
    └──── 每日/挑战任务提供目标感 ←─────────────────────┘
```

### 4. 成长与成就体系

#### 经验值 & 等级
- 每完成一个知识点获得 EXP
- 积累升级：学徒 → 入门 → 进阶 → 精通 → 大师
- 等级影响个人主页展示和排行榜

#### 成就徽章
- **里程碑成就**：完成第一门课全部知识点、探索率达到 50%
- **探索型成就**：发现 5 条暗道、连续学习 7 天
- **稀有成就**：全图谱精通、最短路径挑战者、某区域首位探索者

### 5. 社交与竞争系统

#### 排行榜
- **探索率排行**：按大陆地图探索百分比排名
- **连续打卡排行**：连续学习天数
- **周/月 EXP 排行**：阶段性学习量

#### 学习小组（组队探索）
- 组建探险队（3-5 人），协作开拓地图
- 队伍成员的已探索区域可共享可见（不共享掌握度）
- 组队任务：全队共同完成某区域探索

#### 知识点"占领"
- 某个知识点最先精通的人可以在地点上"插旗"留名
- 查看地点时展示首位探索者和到达人数

#### 好友系统
- 查看好友的地图探索进度
- 互相推荐知识点（"我觉得这个地点值得一去"）

### 6. 动态推荐算法

基于图谱拓扑结构 + 学习状态 + 游戏化数据，推荐策略包括：
- **前置就绪推荐**：所有前置知识已掌握的知识点（优先推荐）
- **邻近扩展推荐**：已掌握节点的 RELATED_TO 邻居
- **薄弱补强推荐**：掌握程度低的前置节点（回溯补漏）
- **路径规划**：给定目标知识点，计算最短学习路径
- **任务驱动推荐**：结合当前进行中的任务推荐下一步

### 7. LLM 辅助（LangChain）

- 知识点讲解：基于图谱上下文生成结构化讲解
- 关联分析："学了X之后为什么要学Y"的路径解释
- 学习建议：根据当前状态和任务进度生成个性化学习计划
- 答疑：Graph-RAG 模式，用图谱关系作为上下文回答问题

### 8. 前端可视化实现

| 要素 | 实现方式 |
|------|---------|
| 地图渲染 | D3.js 力导向图/层次布局，课程级用区域色块划分 |
| 迷雾效果 | SVG filter + 高斯模糊，按节点状态动态调整透明度 |
| 解锁动画 | 迷雾消散过渡动画（opacity transition） |
| 暗道发现 | 虚线→实线动画 + 弹窗提示 |
| 缩放交互 | d3-zoom，大陆全景到知识点细节的多级缩放 |
| 成就弹窗 | 完成任务/获得徽章时的全屏动画提示 |

## 关键技术栈

| 层级 | 技术 | 用途 |
|------|------|------|
| 图数据库 | **Neo4j** | 存储知识图谱（课程、知识点、关系） |
| LLM 编排 | **LangChain** | 编排 LLM 与 Neo4j 交互，生成讲解与推荐 |
| 后端 | **Python + FastAPI** | API 服务、推荐算法、任务系统、用户状态管理 |
| 前端 | **React + D3.js** | 知识大陆地图可视化、游戏化交互界面 |
| 数据库 | **SQLite/PostgreSQL** | 用户信息、学习记录、成就数据、社交关系 |

## 项目难点

1. **知识图谱的质量与粒度**：知识点拆分粒度需要适中，太粗失去推荐意义，太细导致图谱过于复杂
2. **跨课程关联建模**：不同课程间的知识关联（如数据结构的图论 vs 离散数学的图论）
3. **推荐算法的有效性**：如何平衡"按部就班"与"兴趣驱动"的学习路径
4. **冷启动问题**：新用户没有学习记录时的初始推荐策略
5. **游戏化平衡**：激励机制不能喧宾夺主，核心仍是有效学习
6. **多专业扩展**：图谱 Schema 需足够抽象，支持不同专业的知识体系差异

## 项目性质

课程创业项目，本学期持续迭代开发。

## 文件结构

```
terranova/
├── CLAUDE.md          # 项目说明（本文件）
├── memory/            # 对话记录与项目记忆
│   └── YYYY-MM-DD-对话记录.md
└── src/               # 源代码（待创建）
    ├── backend/       # FastAPI 后端
    │   ├── api/       # API 路由
    │   ├── models/    # 数据模型
    │   ├── services/  # 业务逻辑
    │   │   ├── recommend.py   # 推荐算法
    │   │   ├── quest.py       # 任务系统
    │   │   ├── achievement.py # 成就系统
    │   │   └── social.py      # 社交系统
    │   └── graph/     # Neo4j 图谱构建与查询
    ├── frontend/      # React 前端
    │   ├── components/
    │   │   ├── Map/           # 知识大陆地图组件
    │   │   ├── QuestPanel/    # 任务面板
    │   │   ├── Achievement/   # 成就展示
    │   │   ├── Leaderboard/   # 排行榜
    │   │   └── Profile/       # 个人主页
    │   └── pages/
    └── recommend/     # 推荐算法
```

## 对话历史

所有与 Claude 的交互记录保存在 `memory/` 文件夹中，按日期命名。
